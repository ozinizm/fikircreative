# Environment Configuration
NODE_ENV=development

# Database (Development - SQLite is recommended for local development)
DATABASE_URL="file:./dev.db"

# Database Options for Production:
# MySQL (cPanel): DATABASE_URL="mysql://user:password@localhost:3306/dbname"
# PostgreSQL (Vercel): DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"

# Admin Default Credentials (Change in production!)
ADMIN_EMAIL="admin@fikir.agency"
ADMIN_PASSWORD="Admin123!"

# Optional: Email Configuration
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Optional: Rate Limiting
RATE_LIMIT_MAX="100"
RATE_LIMIT_WINDOW="60000"
